version: "3"
services:
  portainer-agent:
    image: portainer/agent:latest
    ports:
      - 9001:9001
    volumes:
    - {{ portainer_data_dir }}/portainer-agent:/data
    - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped